# Optional Video Surprise
Place your optional birthday surprise video here as:
- `birthday-video.mp4`

Supported formats: MP4.
Ensure `videoEnabled: true` is configured in `src/config/birthdayConfig.ts` to display the video player when the gift opens.
If disabled or missing, the player is automatically hidden, showing only the text message.
